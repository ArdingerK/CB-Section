function selectPlacedStencilFromEvent(event) {
    console.log('Event object:', event);
    if (event && (event.shiftKey || event?.shiftKey)) {
        console.log('Shift key is pressed.');
        // Your existing logic for shift key detection
    } else {
        console.log('Shift key is not pressed.');
    }
    // Other existing logic for the function...
}

// Update the placed stencil pointerdown handler
document.querySelector('.placed-stencil-selector').addEventListener('pointerdown', (event) => {
    selectPlacedStencilFromEvent(event);
});